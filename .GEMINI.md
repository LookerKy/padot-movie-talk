# Padot Movie Awards (.GEMINI)

## 📌 Project Overview
**Padot Movie Awards** is a web movie review archive for the virtual streamer **Padot**. It allows the streamer to record reviews, ratings, and one-liners for movies watched with viewers ("Cinety").

## 🛠 Tech Stack
- **Framework**: Next.js 16 (App Router)
- **Library**: React 19.2.1, **Tiptap** (Rich Text Editor)
- **Styling**: Tailwind CSS v4 (Vanilla CSS for specific glass effects if needed), Glassmorphism focus.
- **Language**: TypeScript
- **Database**: Supabase (PostgreSQL)
- **ORM**: Prisma
- **Storage**: Cloudflare R2
- **Deployment**: Cloudflare Pages
- **External API**: TMDB (The Movie Database) for metadata.

## 🎨 Design Guidelines
- **Theme**: Modern, "Premium", Glassmorphism.
- **Signature Color**: Pale Blue (`#95B9DB`).
- **Aesthetics**: High-quality UI, meaningful micro-animations, blur effects.
- **Target Audience**: Viewers of a female virtual streamer (Cute but Modern/Clean).

## 📂 Project Structure (Monolithic)
```text
src/ or app/
  (auth)/       -> Login, Register
  (dashboard)/  -> /admin path
  (public)/     -> /awards, /calendar (Main views)
  api/          -> Route handlers
  components/   -> Reusable basic UI
  lib/          -> Singletons (Prisma, Supabase, TMDB helpers)
```

## 📝 Coding Conventions
- **Naming**: PascalCase for Components, camelCase for functions/vars.
- **Routing**: Use Next.js 16 App Router Groups `(group)`.
- **State**: Server Components by default. Client Components only when interactivity is needed.
- **Forms**: Use Server Actions where possible for data mutation.

## ⚠️ Critical Rules
- **Formatting**: Always format code blocks in Markdown.
- **Language**: Respond to the user in **Korean**.
- **Supabase Free Tier**: Be mindful of connection limits and inactivity pauses.
- **Performance**: Optimize images (use standard `next/image` with unoptimized if using Cloudflare or specialized loader).

## ✅ Implemented Features
- **Review Creation Flow**: TMDB Movie Search -> Metadata Prefill -> Editor.
- **Rich Text Editor**: Custom Tiptap implementation with Font Size (Dropdown), Auto-grow, and Bubble Menu.
- **Tag System**: Linear-style tag picker with database integration.
- **Dotchelin Badge**: Custom toggle with premium badge icon.
